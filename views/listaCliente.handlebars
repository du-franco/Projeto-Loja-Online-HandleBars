<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="css/estilo.css">
    <title>Cadastro de Clientes</title>
</head>

<body>

    <section class="conteudo">
        <table border=1 id="tabela">
            <thead>
                <th>Nome</th>
                <th>Endereço</th>
                <th>Login</th>
                <th>Senha</th>
                <th>Ações</th>
            </thead>
            {{!-- aqui começamos a criar o body da tabela --}}
            <tbody id="tbody">
                {{!-- o #each é semelhante foreach que tínhamos no javascript, ou seja, percorre um array elemento por elemento --}}
                {{#each resultado}}
                <tr>
                    {{!-- estamos criando cada coluna com os atributos de cada elemento do array (um a um) --}}
                    <td>
                        {{_id}}
                    </td>
                    <td>
                        {{nome}}
                    </td>
                    <td>
                        {{valor}}
                    </td>
                    <td>
                        <img src="img/edit.png" alt="">
                       <a href="/deletarProduto/{{_id}}" ><img src="img/delete.png" alt=""></a>
                    </td>
                </tr>
                {{!-- aqui fechamos o each --}}
                    {{/each}}
            </tbody>
        </table>
    </section>
</body>

</html>